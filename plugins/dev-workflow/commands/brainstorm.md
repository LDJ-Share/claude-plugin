---
description: Start a design conversation for creative work — explore intent one question at a time, then produce a spec doc for planning.
argument-hint: [topic or feature, optional]
---
Invoke the `brainstorming` skill and run it for: $ARGUMENTS

Explore project context first, ask clarifying questions ONE AT A TIME
(multiple-choice when possible), propose 2–3 approaches with a recommendation, and
save the approved spec to `docs/superpowers/specs/YYYY-MM-DD-<topic>-design.md`.
On approval, hand off to `/plan` (the `writing-plans` skill).
